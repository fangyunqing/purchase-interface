server:
  port: 9092
  servlet:
    context-path: /LT

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com.hf.project.**.entity


spring:
  datasource:
    username: ncuser
    password: hfyd123.
    url: jdbc:sqlserver://192.168.8.191:1433;DatabaseName=GKSERP_TEST
    driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDriver
    type: com.alibaba.druid.pool.DruidDataSource
    initialSize: 5
    minIdle: 5
    maxActive: 20
    maxWait: 6000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    vpoolPreparedStatements: true
    maxPoolPreparedStatementPerConnectionSize: 20
    filters: stat,wall,log4j2
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000
  mail:
    # 配置 smtp 服务器地址
    host: smtp.exmail.qq.com
    # 发送者邮箱
    username: yunqing.fang@huafeng-cn.com
    # 配置密码
    password: xLHB3H8eC3xuvqMA
    # 端口号465或587
    port: 465
    # 默认的邮件编码为UTF-8
    default-encoding: UTF-8
    # 配置SSL 加密工厂
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
            socketFactory:
              class: com.sun.mail.util.MailSSLSocketFactory
              fallback: false
        debug: false


purchasing-cloud:
  request-property:
    sys-code: LT
    sys-name: 蓝天面料(LT)
    test-mode: false
    tenant-id: c979tew3
    purchase-arrival: false
    purchase-in: false
    purchase-contract: false
    dictInsert: false
    org-name: 福建华峰运动用品科技有限公司莆田分公司
