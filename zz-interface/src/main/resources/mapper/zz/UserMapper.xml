<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hf.project.common.mapper.common.UserMapper">

    <select id="query" parameterType="java.lang.String" resultMap="UserMap">
        SELECT
		USER_ID,
	    USER_NAME2
        FROM
        sd_user
        WHERE USER_ID = #{workCode}
    </select>

    <resultMap id="UserMap" type="User">
        <id column="USER_ID" jdbcType="VARCHAR" property="userCode"/>
        <result column="USER_NAME2" jdbcType="VARCHAR" property="userName"/>
    </resultMap>

</mapper>
